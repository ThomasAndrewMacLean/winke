import React from 'react';
import { Section } from '../styles';

const HeroSection = () => {
  return (
    <Section id="home">
      <h2>Hero</h2>
    </Section>
  );
};

export default HeroSection;
